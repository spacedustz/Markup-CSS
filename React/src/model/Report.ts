export interface Report {
    day: number
    name: string
    desc: string
}

export interface Day1Props {
    title: string
    subTitle: string[]
    favoriteFood: string[]
    questions: string[]
    info: string[]
}